import { recommendMostPopularProgramNames } from "../recommendMostPopularProgramNames"

describe("recommend Most Popular Program Names service", () => {
  it("should be defined", () => {
    expect(recommendMostPopularProgramNames).toBeDefined()
  })

  // @TODO add tests
  it.todo("add more tests")
})
